import Header from '../layout/Header';

const Preloader = ({setLoader}) => {
  return (
    <div style={{ backgroundColor: "#040407" }}>
        <Header/>
    </div>
  );
};
export default Preloader;
